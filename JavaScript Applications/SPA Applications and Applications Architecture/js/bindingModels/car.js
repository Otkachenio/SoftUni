var CarInputModel = (function () {
    function CarInputModel(brand, model, year) {
        this.brand = brand;
        this.model = model;
        this.year = year;
    }

    return CarInputModel;
})();